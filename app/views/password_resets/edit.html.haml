%h1 Reset Your Password

- tagged_form_for(@user, :url => password_reset_path(:id => params[:id]), :html => {:method => :put}) do |f|
  = f.error_messages
  = f.password_field :plain_password
  = f.password_field :plain_password_confirmation
  = f.submit "Do it!"
