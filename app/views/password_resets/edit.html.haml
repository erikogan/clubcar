%h1 Reset Your Password

- tagged_form_for @user, :url => password_reset_path, :method => :put do |f|
  = f.error_messages
  = f.password_field :password
  = f.password_field :password_confirmation
  = f.submit "Update my password and log me in"
