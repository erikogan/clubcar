<h1>New visit for <%= @restaurant.name %></h1>

<%= error_messages_for :visit %>

<% tagged_form_for(:visit, :url => visits_path(@restaurant)) do |f| %>
    <%= f.date_select :date %>
    <%= f.text_field :duration %>
    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', visits_path(@restaurant) %>