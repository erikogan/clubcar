<h1>New email</h1>

<%= error_messages_for :email %>

<% tagged_form_for(:email, :url => emails_path) do |f| %>
    <%= f.text_field :email %>
    <%= submit_tag "Create" %>
<% end %>

<%= link_to 'Back', emails_path %>