<h1>Editing email</h1>

<%= error_messages_for :email %>

<% tagged_form_for(:email, :url => email_path(@user, @email), :html => { :method => :put }) do |f| %>
  <%= f.text_field :address %>
  <%= submit_tag "Update" %>
<% end %>

<%= link_to 'Show', email_path(@user, @email) %> |
<%= link_to 'Back', emails_path %>