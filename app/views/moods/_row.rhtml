  <tr class="<%= cycle('rowA', 'rowB') %>">
    <td><%= link_to h(row.name), mood_path(@user,row) %></td>
    <td><%= h row.order %></td>
    <td><% if row.active -%>
      <img src="<%= image_path('checked.png') %>"/>
	<% else -%>
      <%= image_submit_tag 'unchecked.png', {:alt => 'Set Current Mood to ' + row.name, :name => 'activate[' + row.id.to_s + ']', :onclick => "this.form.activate_input.value = #{row.id};"} %>
	<% end -%>
    </td>
    
    <td><%= link_to 'Edit', edit_mood_path(@user, row) %></td>
    <td><%= link_to 'Destroy', mood_path(@user, row), :confirm => 'Are you sure?', :method => :delete %></td>
    <td><%= link_to 'Copy', copy_mood_path(@user, row) %></td>
  </tr>
