%h1 Editing tagging

%strong NOTE: This will change the tag for ALL tagged items!

- tagged_form_for(@tag) do |f| 
  .err= f.error_messages
  
  = f.text_field :name

  = f.submit "Update"

= link_to 'Show', @tagging
|
= link_to 'Back', taggings_path
